<Page x:Class="bibl.Pages.LoansPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:bibl.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoansPage">

    <Grid>
        <DataGrid x:Name="LoansGrid" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Книга" Binding="{Binding Book.Title}" Width="*"/>
                <DataGridTextColumn Header="Читатель" Binding="{Binding Abonement.Reader.FullName}" Width="*"/>
                <DataGridTextColumn Header="Дата выдачи" Binding="{Binding LoanDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="150"/>
                <DataGridTextColumn Header="Дата возврата" Binding="{Binding ReturnDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
